{
  "name": "webhook-tester",
  "version": "1.0.0",
  "description": "A self-hostable webhook testing and request inspection tool",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npx ts-node src/server.ts",
    "dev:frontend": "cd frontend/webhook-tester-frontend && npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npx tsc",
    "build:frontend": "cd frontend/webhook-tester-frontend && npm run build",
    "start": "npm run start:backend & npm run start:frontend",
    "start:backend": "cd backend && node dist/server.js",
    "start:frontend": "cd frontend/webhook-tester-frontend && npm run start",
    "install:all": "npm install && cd backend && npm install && cd ../frontend/webhook-tester-frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}